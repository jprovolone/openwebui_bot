version: '3.8'

services:
  bot:
    build: .
    environment:
      - WEBUI_URL=${WEBUI_URL}
      - TOKEN=${TOKEN}
      - OPENWEBUI_API_KEY=${OPENWEBUI_API_KEY}
      - BASE_URL=${WEBUI_URL}/api
    restart: unless-stopped
